// Importação de biblioteca padrão para o uso do teclado e monitor
#include <stdio.h>

int main() {
    // para exibir uma mensagem na tela iremos usar
    // o comando printf (exibir formatando)
    printf("Hello, Word!\n");
    // informar a o compilador que o programa 
    // acabou 
    return 0;
}